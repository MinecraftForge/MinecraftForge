--- ../src_base/minecraft/net/minecraft/inventory/ContainerBeacon.java
+++ ../src_work/minecraft/net/minecraft/inventory/ContainerBeacon.java
@@ -6,6 +6,9 @@
 import net.minecraft.entity.player.InventoryPlayer;
 import net.minecraft.item.ItemStack;
 import net.minecraft.tileentity.TileEntityBeacon;
+import net.minecraftforge.common.MinecraftForge;
+import net.minecraftforge.event.Event;
+import net.minecraftforge.event.container.ContainerInteractEvent;
 
 public class ContainerBeacon extends Container
 {
@@ -90,7 +93,13 @@
 
     public boolean canInteractWith(EntityPlayer par1EntityPlayer)
     {
-        return this.theBeacon.isUseableByPlayer(par1EntityPlayer);
+        ContainerInteractEvent event = new ContainerInteractEvent(this, par1EntityPlayer);
+        MinecraftForge.EVENT_BUS.post(event);
+        if (event.isCanceled())
+        {
+            return false;
+        }
+        return event.interact == Event.Result.ALLOW ? true : event.interact == Event.Result.DEFAULT ? this.theBeacon.isUseableByPlayer(par1EntityPlayer) : false;
     }
 
     /**
