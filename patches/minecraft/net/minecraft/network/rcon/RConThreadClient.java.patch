--- ../src_base/minecraft/net/minecraft/network/rcon/RConThreadClient.java
+++ ../src_work/minecraft/net/minecraft/network/rcon/RConThreadClient.java
@@ -54,7 +54,11 @@
 
                 BufferedInputStream bufferedinputstream = new BufferedInputStream(this.clientSocket.getInputStream());
                 int i = bufferedinputstream.read(this.buffer, 0, 1460);
-
+                // MCPC+ start - Update RConThreadClient to fix 100% CPU load with RCON
+                if (i==-1) {
+                    this.running = false;
+                }
+                // MCPC+ end
                 if (10 <= i)
                 {
                     byte b0 = 0;
