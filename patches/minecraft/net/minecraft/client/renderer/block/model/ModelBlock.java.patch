--- ../src-base/minecraft/net/minecraft/client/renderer/block/model/ModelBlock.java
+++ ../src-work/minecraft/net/minecraft/client/renderer/block/model/ModelBlock.java
@@ -28,7 +28,7 @@
 import org.apache.logging.log4j.Logger;
 
 @SideOnly(Side.CLIENT)
-public class ModelBlock
+public class ModelBlock implements net.minecraftforge.common.capabilities.ICapabilityProvider
 {
     private static final Logger field_178313_f = LogManager.getLogger();
     @VisibleForTesting
@@ -45,6 +45,7 @@
     public ModelBlock field_178315_d;
     @VisibleForTesting
     protected ResourceLocation field_178316_e;
+    private final net.minecraftforge.common.capabilities.CapabilityDispatcher capabilities;
 
     public static ModelBlock func_178307_a(Reader p_178307_0_)
     {
@@ -65,8 +66,15 @@
         this.field_178316_e = p_i46573_1_;
         this.field_178320_j = p_i46573_6_;
         this.field_187968_k = p_i46573_7_;
+        this.capabilities = net.minecraftforge.event.ForgeEventFactory.gatherCapabilitiesNotNull(new net.minecraftforge.client.event.ClientAttachCapabilitiesEvent.ModelBlock(this));
     }
 
+    public ModelBlock(@Nullable ResourceLocation parentLocationIn, List<BlockPart> elementsIn, Map<String, String> texturesIn, boolean ambientOcclusionIn, boolean gui3dIn, ItemCameraTransforms cameraTransformsIn, List<ItemOverride> overridesIn, net.minecraft.nbt.NBTTagCompound capabilities)
+    {
+        this(parentLocationIn, elementsIn, texturesIn, ambientOcclusionIn, gui3dIn, cameraTransformsIn, overridesIn);
+        if(capabilities != null) this.capabilities.deserializeNBT(capabilities);
+    }
+
     public List<BlockPart> func_178298_a()
     {
         return this.field_178314_g.isEmpty() && this.func_178295_k() ? this.field_178315_d.func_178298_a() : this.field_178314_g;
@@ -227,6 +235,25 @@
         }
     }
 
+    public net.minecraftforge.common.capabilities.CapabilityDispatcher getCapabilities()
+    {
+        return capabilities;
+    }
+
+    @Override
+    public boolean hasCapability(net.minecraftforge.common.capabilities.Capability<?> capability, net.minecraft.util.EnumFacing facing)
+    {
+        if (getCapability(capability, facing) != null)
+            return true;
+        return capabilities == null ? false : capabilities.hasCapability(capability, facing);
+    }
+
+    @Override
+    public <T> T getCapability(net.minecraftforge.common.capabilities.Capability<T> capability, net.minecraft.util.EnumFacing facing)
+    {
+        return capabilities == null ? null : capabilities.getCapability(capability, facing);
+    }
+    
     @SideOnly(Side.CLIENT)
     static final class Bookkeep
         {
@@ -259,7 +286,7 @@
 
                 List<ItemOverride> list1 = this.func_187964_a(p_deserialize_3_, jsonobject);
                 ResourceLocation resourcelocation = s.isEmpty() ? null : new ResourceLocation(s);
-                return new ModelBlock(resourcelocation, list, map, flag, true, itemcameratransforms, list1);
+                return new ModelBlock(resourcelocation, list, map, flag, true, itemcameratransforms, list1, net.minecraftforge.client.ForgeHooksClient.capabilitiesJsonToNbt(ModelBlock.field_178319_a.toJson(jsonobject.get("capabilities"))));
             }
 
             protected List<ItemOverride> func_187964_a(JsonDeserializationContext p_187964_1_, JsonObject p_187964_2_)
