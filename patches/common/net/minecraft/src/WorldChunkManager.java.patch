--- ../src_base/common/net/minecraft/src/WorldChunkManager.java	0000-00-00 00:00:00.000000000 -0000
+++ ../src_work/common/net/minecraft/src/WorldChunkManager.java	0000-00-00 00:00:00.000000000 -0000
@@ -3,8 +3,12 @@
 import cpw.mods.fml.common.Side;
 import cpw.mods.fml.common.asm.SideOnly;
 import java.util.ArrayList;
+import java.util.Arrays;
 import java.util.List;
 import java.util.Random;
+import java.util.Set;
+
+import com.google.common.collect.Sets;
 
 public class WorldChunkManager
 {
@@ -18,9 +22,12 @@
 
     /** A list of biomes that the player can spawn in. */
     private List biomesToSpawnIn;
+    
+    private static WorldChunkManager INSTANCE;
 
     protected WorldChunkManager()
     {
+    	INSTANCE = this;
         this.biomeCache = new BiomeCache(this);
         this.biomesToSpawnIn = new ArrayList();
         this.biomesToSpawnIn.add(BiomeGenBase.forest);
@@ -257,4 +264,20 @@
     {
         this.biomeCache.cleanupCache();
     }
+    
+    public static void addBiome(BiomeGenBase biome)
+    {
+    	List biomes = new ArrayList();
+        biomes.addAll(INSTANCE.biomesToSpawnIn);
+        biomes.add(biome);
+        INSTANCE.biomesToSpawnIn = biomes;
+    }
+    
+    public static void removeBiome(BiomeGenBase biome)
+    {
+    	List biomes = new ArrayList();
+        biomes.addAll(INSTANCE.biomesToSpawnIn);
+        biomes.remove(biome);
+       	INSTANCE.biomesToSpawnIn = biomes;
+    }
 }
